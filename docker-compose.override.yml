services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    image: ai-agent-video:local
    environment:
      # 存储走火山引擎 TOS（配置在 .env，含 TOS_PUBLIC_BASE_URL 公网域名，
      # 供腾讯云 AIGC 拉取首帧/参考图）。MinIO 容器（waoowaoo-minio）保留运行，
      # 仅用于一次性迁移旧数据（scripts/migrate-minio-to-tos.ts），不再作为业务存储。
